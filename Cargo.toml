[package]
name = "ascii"
version = "0.1.2"
edition = "2024"
repository = "https://github.com/InterstelarDev/img_to_ascii"

[dependencies]
image = "0.25.10"
terminal_size = "0.4.4"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
