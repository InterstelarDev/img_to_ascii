[package]
name = "ascii"
version = "0.1.1"
edition = "2024"
repository = "https://github.com/InterstelarDev/img_to_ascii"

[dependencies]
image = "0.25.10"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
